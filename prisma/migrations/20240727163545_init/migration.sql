-- CreateTable
CREATE TABLE `Make` (
    `make_id` INTEGER NOT NULL AUTO_INCREMENT,
    `make_name` VARCHAR(191) NOT NULL,

    PRIMARY KEY (`make_id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
